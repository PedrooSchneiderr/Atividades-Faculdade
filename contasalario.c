#include <stdio.h>

    int main(){
    float salario = 100;
    float aumento = 0.5;
            float nsalario = (salario * aumento);
            float nalario = nsalario + salario;
            printf("%f", nsalario); 
    return 0;
    }
/*Esse código em C calcula o novo salário após um aumento de 50% e imprime esse valor, 
aplicando o aumento ao salário original e exibe o resultado*/
